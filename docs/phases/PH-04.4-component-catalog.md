# PH-04.4 — 컴포넌트 카탈로그 v2 (SCREEN-FLOW 전체 커버리지)

> **의존:** [PH-04.3](PH-04.3-design-assembly-retrofit.md)(완료, 6종 프리미티브 소급 감사) · [PH-05.1](PH-05.1-core-loop-remediation.md)·[PH-06.1](PH-06.1-carryover-requeue.md)·[PH-07](PH-07-onboarding.md)·[PH-08](PH-08-discharge.md)·[PH-09](PH-09-settings-northstar.md)(전부 완료 — 이 위상이 소급 감사·리팩터하는 산출물)
> **SSOT:** [`DESIGN-SYSTEM.md`](../DESIGN-SYSTEM.md)(조립 규약, §5·§6 신설 대상) · [`DESIGN-TOKENS.md` §3 결정#6·§5](../DESIGN-TOKENS.md#3-semantic-토큰-화면은-여기부터-참조)(가드레일 바인딩) · [`SCREEN-FLOW.md` §1](../SCREEN-FLOW.md#1-화면상태-인벤토리)(화면 인벤토리 15개) · [`CLAUDE.md` §1·§2·§6](../../CLAUDE.md)(정체성 경계·불변 규칙·신규 기능 체크리스트) · [`common/coding-style.md`](../../../../.claude/rules/ecc/common/coding-style.md)(DRY)
> **전역 규칙:** [phases/README.md §0](README.md#0-전역-규칙) DO NOT CHANGE·Runnable State·**컴포넌트 카탈로그 게이트**(이 위상이 신설한 규칙, README §0 참조) 정의는 여기 반복 안 함.

## Goal

`DESIGN-SYSTEM.md`에 §5(컴포넌트 카탈로그)·§6(조립 레시피)이 신설되어 SCREEN-FLOW §1의 15개 화면 전부가 "카탈로그에 있는 컴포넌트의 조합"만으로 설명 가능하고, 감사로 발견된 5개 미등재 컴포넌트(`TextInput`·`TimerDisplay`·`StateChip`·`BonusCard`·`NorthStarBadge`)가 `src/components`에 구현되어 기존 페이지의 ad-hoc CSS를 대체한 상태.

## 배경 — 어쩌다 이 위상이 필요해졌나

PH-04.3은 PH-04가 만든 **6종 프리미티브**(`Button`·`Chip`·`TaskCard`·`EnergyBar`·`OptionRow`·`BottomSheet`)만 소급 감사했다. 그런데 그 이후 완료된 PH-05.1/06.1/07/08/09가 각자 페이지 CSS 모듈로 **회고 인정칩·보너스 카드·타이머 디스플레이·텍스트 입력(3곳 중복)·북극성 배지**를 그때그때 재발명했고, 어느 것도 `DESIGN-SYSTEM.md`에 등재되지 않은 채 각 위상이 "완료" 처리됐다. `phases/README.md §0`의 SPEC 커버리지 게이트는 "SPEC에 있는데 코드가 없는 것"만 잡고, PH-04.3의 소급 감사는 "이미 있는 6종의 배치"만 봤다 — **"코드에는 있는데 카탈로그에 없는 UI 패턴"을 잡는 절차가 없었다.** 이 위상이 그 절차(컴포넌트 카탈로그 게이트, `phases/README.md §0` 신설)와 이번 세대의 소급분을 함께 처리한다.

---

## Phase 1 — UX 근거 수립 (컴포넌트별 워크시트)

> **이 절이 이 위상의 핵심이다.** 각 신규 컴포넌트는 "이 토큰을 쓴다"가 아니라 **"왜 이 형태이고 왜 다른 형태가 아닌가"**를 페르소나 K(CLAUDE.md 설계 기준선 — 약물 중단·우울 동반·배터리 5%·목표가 의무↔열망으로 찢김)의 구체적 마찰 시나리오에 대고 검증한다. 기각한 대안을 남기는 이유: "왜 더 화려한/기능적인 버전을 안 만들었나"가 나중에 재질문되지 않도록.

### 1-1. `TextInput`

| 항목             | 내용                                                                                                                                                                                                                                                                                                                       |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 현재 재발명 위치 | `DashboardPage.module.css .input`(1-A 아무거나 입력) · `NorthStarPage.module.css .input`×2(1-B 열망·의무) · `FocusPage.module.css .captureInput`(5-A 딴생각, textarea) — 3개 파일에 독립 구현, 포커스 스타일 상호 드리프트 위험                                                                                            |
| K 마찰 시나리오  | 배터리 5%, 손 떨림·피로로 오타가 잦다. 입력창이 "필수" 표시나 빨간 밑줄로 반응하면 "또 틀렸다"는 즉각적 자기비난 트리거 — RSD(거부민감성) 정중앙을 찌른다                                                                                                                                                                  |
| 기각한 대안      | (a) HTML5 `required` 기본 검증 팝업 — 브라우저 시스템색(빨강)이 앱 색채계 밖에서 튀어나와 처벌색 없음(DESIGN-TOKENS §5-2)을 우회 위반. (b) 실시간 글자수 카운터 — "평가받는 칸"이라는 인상, §4 톤 위반. (c) 흐린 placeholder로 "이미 답이 있는 것처럼" 보이는 스켈레톤 스타일 — 뭘 써야 할지 판단을 더 시켜 결정 피로 증가 |
| 선택한 형태      | `surface.raised`+`border.default` 무테두리에 가까운 단일 상태. 포커스는 `border` → `action.ink`(텍스트색 전환, 배경은 불변 — CTA 아님). **컴포넌트 props 타입에 `required`/`error`/`maxLength` 자체가 없음** — 있으면 언젠가 누군가 쓴다                                                                                   |
| 가드레일 근거    | CLAUDE §2 결정 피로 차단 · §4 톤 · DESIGN-TOKENS §5-2 처벌색 없음                                                                                                                                                                                                                                                          |

### 1-2. `TimerDisplay`

| 항목             | 내용                                                                                                                                                                                                                                                        |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 현재 재발명 위치 | `FocusPage.module.css .timer` / `.label`(화면 5 집중)                                                                                                                                                                                                       |
| K 마찰 시나리오  | 우울 동반 상태의 시간감각 왜곡 — 15분이 "영원처럼" 느껴진다. 진행률 링·퍼센트가 있으면 15분 내내 "얼마나 남았나"를 확인하는 시계-감시 루프에 갇힌다 — 이건 "시작 훈련"이 아니라 인내 시험이 되어 §1 정체성 경계(플래너/타이머 앱 아님)를 넘는다             |
| 기각한 대안      | (a) 원형 프로그레스 바 — 방전 모드도 "정상과 시각적으로 동일"해야 하는데, 진행률이 두드러지면 방전에서도 "덜 채워짐"이 지각됨. (b) 초 단위 카운트다운(빠르게 바뀌는 숫자) — 초조함 유발, 담백함 위반. (c) 남은 블록 수/오늘 총 분량 병기 — §5 성적표 프레임 |
| 선택한 형태      | 큰 숫자(분 단위) + 지금 하는 동사 라벨만. 진행률 표시 완전 배제. `mode-focus` 다크 오버레이로 감각 자극을 낮춤. `running`/`paused`(`dark.bgDeep`)/`discharge`(mode-discharge, 문구만 다름 — 크기·서체 동일) 3 variant                                       |
| 가드레일 근거    | CLAUDE §1 정체성 경계 · §2 결정 피로 차단 · §6 체크리스트 6번("집중 화면에 볼거리 추가 금지")                                                                                                                                                               |

### 1-3. `StateChip`(회고 인정칩)

| 항목             | 내용                                                                                                                                                                                                                                          |
| ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 현재 재발명 위치 | `RetroPage.tsx`의 로컬 `RecognitionChip` 함수                                                                                                                                                                                                 |
| K 마찰 시나리오  | "못 했다"를 인정하는 순간 자체가 이미 위험 지대다. 그린/퍼플 색이 회고 화면 밖(대시보드·에너지 바)으로 새면 "성적표 도장"처럼 각인되고, 방금 겪은 미완료가 다른 화면에서 다시 눈에 띄면 하루 종일 반추(rumination)를 유발한다                 |
| 기각한 대안      | (a) 완료/미완료 아이콘을 에너지 바 옆에 상시 표기 — §2 실패 무처벌 정면 위반. (b) 대시보드 헤더에 "오늘 완료 N·미완료 M" 요약 — 완전한 성적표 프레임                                                                                          |
| 선택한 형태      | 회고 화면에만 존재(파일 위치·export 스코프로 물리적 제한 — 문서화만으론 재사용을 못 막는다는 것이 DESIGN-TOKENS §3 결정#6이 이미 지적한 경계). 카피는 "완료"/"이어감"(실패·미완료라는 단어를 UI 텍스트에 노출하지 않음, 코드 주석에서만 사용) |
| 가드레일 근거    | DESIGN-TOKENS §3 결정#6 경계 조건 · CLAUDE §2·§4                                                                                                                                                                                              |

### 1-4. `BonusCard`

| 항목             | 내용                                                                                                                                                                                                          |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 현재 재발명 위치 | `RetroPage.tsx`의 로컬 `BonusCard` 함수                                                                                                                                                                       |
| K 마찰 시나리오  | 예측이 빗나갔을 때 "졌다"는 느낌을 주면 안 된다 — 앱이 유일하게 "관객 없는" 공간이어야 하는데(§3 anti-goal), 빗나감에 대한 어떤 시각 반응(회색 카드, 흐려진 텍스트)이라도 있으면 "심판당했다"는 인상을 남긴다 |
| 기각한 대안      | (a) 빗나감 시 옅은 회색 카드로 "괜찮아요" 텍스트 — 있다는 사실 자체가 신호. 아예 없어야 진짜 무표시. (b) 적중률 누적 표시 — 게임화, §3 슬롯머신 anti-goal 정면 위반                                           |
| 선택한 형태      | `hit=false` → `null` 반환(컴포넌트 시그니처 자체가 조건부 렌더를 강제, 빈 카드·placeholder 없음)                                                                                                              |
| 가드레일 근거    | SCREEN-FLOW §3-2 · DESIGN-TOKENS §5-4                                                                                                                                                                         |

### 1-5. `NorthStarBadge`

| 항목             | 내용                                                                                                                                                                                                        |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 현재 재발명 위치 | `DashboardPage.tsx`/`SettingsPage.tsx` 각자 로컬 표시(로직은 `north-star-selectors.ts` 공유, UI는 비공유)                                                                                                   |
| K 마찰 시나리오  | 목표가 의무↔열망으로 찢긴 상태에서, 배지가 "진행 중" 뉘앙스(은은한 애니메이션·강조 테두리)를 조금이라도 풍기면 "이것도 또 관리해야 할 대상이구나"라는 경계심을 유발한다(§1 정체성 경계 — 플래너 아님)       |
| 기각한 대안      | (a) 배지를 탭하면 바로 수정 화면으로 직행 — 마찰은 줄지만 "관리 대상"이라는 인상이 강화됨. 설정 경유는 SPEC §9가 이미 의도한 마찰. (b) subtle 그라디언트 배경으로 강조 — 장식이 "정적 좌표"라는 의미를 왜곡 |
| 선택한 형태      | 순수 정적 텍스트. **`onClick` prop이 타입에 존재하지 않음**(코드 리뷰가 실수로 탭 핸들러를 추가하는 걸 타입 레벨에서 차단)                                                                                  |
| 가드레일 근거    | SPEC §9 "진행 측정기 아님, 탭 핸들러 없음" · CLAUDE §1·§5                                                                                                                                                   |

### 1-6. 조립 레시피 UX 메모 (신규 컴포넌트 없이 기존 조합으로 충분한 것)

- **`CaptureModal`(5-A 딴생각)** = `BottomSheet` + `TextInput multiline` + `Button`. K 시나리오: 타이머가 안 멈추는 상태에서 입력하므로 인지 부하가 이미 높다 — 새 primitive를 만들지 않고 기존 `BottomSheet`(elevation.sheet)를 그대로 써서 "이건 또 다른 종류의 화면"이라는 학습 비용을 추가하지 않는다.
- **`PauseOverlay`(5-B 일시정지)** = `BottomSheet` + `Button`×2(재개/그만하기). "그만하기"는 죄책감 어휘 없는 중립 표현 유지(이미 구현됨, 카탈로그 등재만 필요). 버튼 2개 모두 44×44px 이상 — 배터리 5%·손 떨림 상태의 오탭 방지가 곧 마찰 최소화(§4).
- **`DischargeLink`(방전 진입)** = `Button variant="secondary"` 재사용. 신규 "QuietLink" primitive를 검토했으나 기각 — secondary 버튼이 이미 `action`(테라코타 elevation.cta) 없이 아웃라인만 쓰므로 §5-3 위반 소지가 없고, 새 컴포넌트를 만들면 오히려 "방전 링크는 특별하다"는 시각적 신호가 되어 §4 "고장 라벨 없이 초대 톤만"의 취지와 어긋난다.

---

## Phase 2 — `DESIGN-SYSTEM.md` 확장

### 커버리지 표 (SPEC 커버리지 게이트의 이 위상용 변형 — PH-04.2 선례)

| 화면 ID(SCREEN-FLOW §1) | 필요 컴포넌트                                 | 배정                                        |
| ----------------------- | --------------------------------------------- | ------------------------------------------- |
| 1 면죄부 3화면          | `TaskCard`+`Button`                           | 기존 조합, 카탈로그 §6 조립 레시피에만 등재 |
| 1-A/2 아무거나 입력     | `TextInput`                                   | In-Scope(신규)                              |
| 1-B 북극성 선택         | `TextInput`×2                                 | In-Scope(신규, 재사용)                      |
| 2 대시보드 헤더         | `NorthStarBadge`                              | In-Scope(신규)                              |
| 4 사전 예측             | `OptionRow`                                   | 기존, 조립 레시피 등재만                    |
| 5 집중                  | `TimerDisplay`                                | In-Scope(신규)                              |
| 5-A 딴생각              | `BottomSheet`+`TextInput`                     | 조립 레시피(신규 조합 문서화)               |
| 5-B 일시정지            | `BottomSheet`+`Button`                        | 조립 레시피                                 |
| 6/6′/7/7′ 회고          | `StateChip`+`BonusCard`+`OptionRow`(영점조절) | In-Scope(신규 2)                            |
| 방전 진입/대시보드      | `Button.secondary`+`TaskCard`                 | 조립 레시피(신규 primitive 기각 근거 §1-6)  |
| 9 설정                  | `OptionRow`(알림 on/off)                      | 기존, 조립 레시피 등재만                    |

1. **`DESIGN-SYSTEM.md` §5 컴포넌트 카탈로그 신설** — 기존 §3 elevation 매핑표 형식으로 11종(기존 6 + 신규 5) 전수 표 작성, 각 행에 Phase 1 워크시트의 "가드레일 근거" 열 인용.
2. **`DESIGN-SYSTEM.md` §6 조립 레시피 신설** — SCREEN-FLOW §1 15개 화면 ID별로 "화면 = 프리미티브 조합" 표, 1-6의 기각 근거(신규 primitive 안 만든 이유)도 함께 기록.

## Phase 3 — 가드레일 교차검증

- `DESIGN-TOKENS.md §5-5` QA 체크리스트에 추가: "`StateChip`이 회고 페이지 배럴 밖에서 import되지 않았는가."
- CLAUDE.md §6 체크리스트 6문항 × 신규 5개 컴포넌트 = 30개 항목 전수 통과를 이 파일 Changelog에 기록(완료 선언 직전).

## Phase 4 — 구현(TDD) + 페이지 리팩터

- `src/components/{TextInput,TimerDisplay,StateChip,BonusCard,NorthStarBadge}` 신규(RED→GREEN, 기존 6종과 동일 폴더 구조).
- `DashboardPage`/`NorthStarPage`/`FocusPage`/`RetroPage`/`SettingsPage`의 ad-hoc `<input>`/`<textarea>`/로컬 함수를 신규 컴포넌트로 교체, 중복 CSS 삭제.
- `StateChip`은 `src/components`에 공개 노출하지 않고 `src/pages/RetroPage/` 로컬 유지(또는 ESLint `no-restricted-imports`로 스코프 강제) — Phase 1-3 워크시트의 결론.

## In-Scope

- [ ] (착수 직전 30~50개 항목으로 상세화 — 착수 대상 확정 후)

## DO NOT CHANGE (국소)

- `DESIGN-TOKENS.md`의 모든 값(§9 JSON) — 배치·컴포넌트 카탈로그만 추가, 값 재정의 금지
- `action`/`evidence.fill` 및 그 semantic 매핑
- 기존 6종 프리미티브의 기존 API·토큰 바인딩(PH-04.3 산출물) — 이 위상은 신규 5종 추가 + 페이지 호출부만 교체

## Positive Non-Goals

- 방전톤(`color.calm`) 최종값 확정 — `DESIGN-BRIEF.md` 소유, 이 위상 범위 밖(DESIGN-TOKENS §10-1)
- `evidence.fill` 비텍스트 대비 미해결 항목 재론 — DESIGN-TOKENS §10-4 그대로 유지
- CI(Linux) 도입에 따른 Playwright 스냅샷 재기준선 — 별도 위상

## 수용 기준 (기계 검증만)

**공통:**

- [ ] `npm run typecheck` / `npm run lint` / `npm run test` 전부 exit 0
- [ ] 커버리지 표의 모든 행이 실제 구현과 일치(완료 선언 직전 재확인)

**이 위상 고유:**

- [ ] `DESIGN-SYSTEM.md` §5·§6 신설, 11종 전수 표 존재
- [ ] `src/pages/*`에 원시 `<input>`/`<textarea>` 또는 로컬 재발명 회고칩·보너스카드·타이머 마크업 0건(grep 검증)
- [ ] `BonusCard`/`NorthStarBadge` 조건 미충족 시 `null` 반환 테스트 존재
- [ ] `StateChip`이 회고 페이지 배럴 밖에서 import 불가(lint 또는 파일 위치로 강제) 확인
- [ ] 전체 vitest 회귀 없음(기존 스위트 유지), Playwright 320/768px 왕복 확인

## Runnable-State 커맨드

```
npm run build && npm run test && npx playwright test
```

## Changelog

- **v0.1(2026-07-11)** — 최초 작성(계획만, 착수 대기). 감사 결과 5개 미등재 컴포넌트(`TextInput`·`TimerDisplay`·`StateChip`·`BonusCard`·`NorthStarBadge`) 확정, Phase 1에 페르소나 K 마찰 시나리오·기각한 대안 워크시트 수록. `phases/README.md §0`에 컴포넌트 카탈로그 게이트 신설, `docs/README.md`에 문서 관계도(mermaid) 신설과 짝지어 등록.
