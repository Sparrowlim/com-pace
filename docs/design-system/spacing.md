# 여백 리듬 (Spacing) — `SP`

> 상위: [`../DESIGN-SYSTEM.md`](../DESIGN-SYSTEM.md) 허브 · 값 정본: [`../DESIGN-TOKENS.md §2-8`](../DESIGN-TOKENS.md) (`space.*`)
> 이 문서는 값을 소유하지 않는다 — `space.*` 8종을 **어디에 쓰는가**만 고정한다.

## SP-1. `space.*` 3계층 용도 고정

컴포넌트마다 임의로 값을 고르는 것을 막는다. 새 컴포넌트/화면은 아래 표에서 역할에 맞는 계층의 토큰만 쓴다.

| 계층                                   | 토큰                                                               | 용도                                                                                        |
| -------------------------------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| **카드-내부** (요소 간)                | `space.1`(4px) · `space.2`(6px) · `space.3`(8px) · `space.4`(12px) | 카드 하나 안에서 아이콘-라벨 간격, 제목-본문 마진, 버튼/칩 내부 gap 등 요소끼리의 좁은 간격 |
| **카드-간** (형제 항목 사이)           | `space.5`(16px) · `space.6`(20px)                                  | 리스트/스택에서 반복되는 형제 카드·옵션 행 사이 간격                                        |
| **섹션-간** (카드 자체 외곽 패딩 포함) | `space.7`(24px) · `space.8`(30px)                                  | 대표 카드(`TaskCard`·`BottomSheet`)의 테두리-콘텐츠 외곽 패딩, 화면 내 큰 블록 사이 여백    |

**근거:** 카드의 외곽 패딩은 "이 카드가 화면·주변과 얼마나 호흡하는가"를 결정하는 거시 리듬이라 섹션 간격과 같은 스케일을 공유한다. 카드 내부 요소끼리의 간격은 훨씬 좁은 스케일로 분리해야 "카드 하나"라는 응집력이 시각적으로 유지된다.

## SP-2. 계층 위반 판정

- 내부 패딩(버튼·칩)에 "카드-간" 이상 계층 값을 쓰면 위반 — 카드 응집력이 깨진다.
- 화면 세로 배치(무게중심·앵커)는 여백 리듬이 아니라 [`composition.md`](composition.md)가 소유한다. 이 문서는 "얼마나 벌리는가(간격 크기)"만 다루고, "어디에 두는가(배치)"는 다루지 않는다.

---

이력·소급 감사 기록은 [`CHANGELOG.md`](CHANGELOG.md).
